# QUICK START

1. `npm install`
2. `npm run setup`
3. Edit `.env`
4. `npm run build`
5. `npm start`

## Verify
- `npm run health`
- `npm test`
- `curl http://localhost:3000/api/status`

## Storage
This bot uses local NeDB files under `DB_DIR` (default `./data`).
